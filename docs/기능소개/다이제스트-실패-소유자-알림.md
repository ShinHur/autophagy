# 다이제스트 실패 알림

## 무엇을

다이제스트 생성·전달 실패를 구조화된 상태로 기록하고 소유자(owner)에게 중복 없이 알리는 흐름이다.

## 왜

예약 작업의 실패가 조용하면 사용자는 알림이 없다는 사실조차 알아차리지 못한다.

## 사용 시나리오

전달 단계가 실패하면 민감 본문 없이 실패 종류와 재시도 가능 여부를 알린다. 동일 실패는 안전한 키로 묶어 중복 알림을 피한다. 이후 성공하면 실패 상태를 정상 결과로 오인하지 않는다.

## 관련

`skills/mail/`, `docs/design/`
