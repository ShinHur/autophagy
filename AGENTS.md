# AGENTS.md — 공개 저장소 작업 규약

## 개요

이 저장소는 승인형 개인 자동화와 에이전트 상호운용을 위한 Python 중심 구성요소를 제공한다. 표준 라이브러리 우선 원칙을 따르며, 읽기는 자유롭게 하되 메일·일정·문서·예산·배포처럼 외부 상태를 바꾸는 모든 작업은 **소유자(owner) 승인 게이트**를 거쳐야 한다. 확인할 수 없는 권한·설정·대상·승인은 실행하지 않는다.

## 구조

```
automation/  # 게이트, 워처, 배포·수리·저장 자동화
configs/     # 추적 가능한 정책 시드와 예시 인벤토리
docs/        # 공개 설계, 기능 인벤토리, 기능 소개
prompts/     # 버전 관리되는 프롬프트 자산
skills/      # 독립 스킬 패키지
tests/       # 단위 및 시나리오 검증
```

## 어디를 볼 것인가

| 작업 | 위치 | 비고 |
|---|---|---|
| 공개 설계와 경계 | `docs/design/` | 승인, 배포, 저장, 백엔드 계약의 공개 설계 문서 |
| 기능 범위 | `docs/features.md` | 포함 기능과 의도적으로 제외한 기능의 공개 인벤토리 |
| 기능 사용법 | `docs/기능소개/` | 소유자 관점의 안전한 사용 시나리오 |
| 외부효과 승인 | `automation/interop/` | 해시 바인딩과 fail-closed 경계 |
| 사이트 메일 백엔드 계약 | `docs/design/` 및 `skills/mail/` | 특정 기관 구현이 아닌 교체 가능한 백엔드 경계 |
| 스킬 제작과 배포 | `skills/`, `automation/deploy-skill.sh` | 샌드박스·검토·승인·활성화 절차 |
| 배포 출처 검증 | `automation/deploy_provenance.sh` | 원격 기준과 배포 입력을 대조 |
| 관리형 스킬 | `automation/managed_skills/`, `automation/managed_sync/` | 발행·검증·격리·활성화 경로 |
| 수리 자동화 | `automation/repair/` | 재현·패치·검증·승인·검토 요청 경로 |
| 정책 시드 | `configs/` | 런타임 상태가 아닌 검토 가능한 기본값 |

## 코드 맵

| 구성요소 | 종류 | 위치 | 역할 |
|---|---|---|---|
| 승인 상호운용 계층 | 모듈 | `automation/interop/` | 외부효과 게이트, 승인 생명주기, 전송 어댑터 |
| 스킬 게이트 | 모듈 | `automation/skill_gate.py` | 스킬 검토·승인·활성화 제어 |
| 배포 출처 검사 | 스크립트 | `automation/deploy_provenance.sh` | 배포할 바이트가 승인된 원격 기준과 일치하는지 검사 |
| 릴리스 수렴 | 스크립트 | `automation/land.sh` | 검토된 변경의 수렴과 배포 상태 확인 |
| 배포 체크아웃 검사 | 스크립트 | `automation/checkout_mirror_probe.sh` | 미러의 dirty·ahead·behind 상태를 비파괴적으로 감지 |
| 수리 워크플로 | 패키지 | `automation/repair/` | 격리된 재현·패치·회귀·승인 경계 |
| 저장 목적지 라우팅 | 스킬 | `skills/doctype/` | 요청 목적과 모호성을 보수적으로 판정 |
| 사이트 메일 경계 | 스킬 | `skills/mail/` | 공급자별 구현을 공개 계약 뒤에 격리 |
| 관리형 스킬 | 패키지 | `automation/managed_skills/` | 서명된 릴리스 발행과 매니페스트 검증 |
| 개인 RAG 인제스트 | 패키지 | `automation/rag_ingest/` | 내용 해시 기반 멱등 인제스트와 민감도 경계 |

## 핵심 불변식

- **표준 라이브러리 우선.** 타입을 명확히 하고, 불가피한 선택 의존성은 지연 import하며 사용 불가 시 안전하게 거부한다.
- **외부효과는 승인 후에만 실행.** 초안·대상·명령·첨부·패치 등 실행 입력을 안정된 해시에 결속하고, 실행 직전에 다시 검증한다.
- **성공 후 상태 마킹.** claim → 작업 → 성공 기록 순서를 지킨다. 실패한 작업은 재시도 가능하게 release한다.
- **fail-closed.** 설정 파싱, 권한 확인, 대상 식별, 해시 대조, 전송 결과 중 하나라도 불명확하면 변경하지 않는다.
- **추적 설정은 불변 시드.** 운영 중 상태·원장·캐시·자격증명은 `$AUTOPHAGY_RUNTIME_ROOT` 같은 체크아웃 밖의 보호된 경로에 둔다. 추적 파일을 런타임에 바꾸면 배포 기준이 오염되고 수렴이 막힌다.
- **커밋과 배포는 다르다.** 활성 릴리스의 해시 또는 실제 smoke 검증으로 배포를 판정한다. 저장소 상태만으로 실행 중인 버전을 추정하지 않는다.
- **관리형 스킬 이름은 `managed-`로 예약한다.** 일반 스킬과 관리형 스킬의 이름 충돌은 어느 쪽도 우선하지 않고 거부한다.
- **공개 문서는 구현과 함께 갱신한다.** 기능·계약·제약이 변하면 관련 `AGENTS.md`, `SKILL.md`, 설계 문서, 기능 소개, 공개 인벤토리를 같은 변경 단위에서 갱신한다. 낡은 문서는 잘못된 자동화를 유도한다.

## 승인 규약

- ✅는 승인·확정·실행, ⛔는 거부·취소를 뜻한다.
- 소유자(owner)의 비봇 반응만 인정한다. 두 반응이 함께 있으면 ⛔를 우선한다.
- 승인 메시지는 실행하려는 초안의 해시를 참조해야 하며, 확인할 수 없으면 실행하지 않는다.
- 승인 UI에는 두 반응을 미리 붙인다. 텍스트 명령은 호환용 보조 수단일 뿐, 별도 비밀 채널로 재확인을 유도하지 않는다.
- 같은 논리 요청에는 라이브 승인 메시지가 정확히 하나만 있어야 한다. 같은 `action_hash`를 중복 게시하거나 저장된 메시지 식별자를 새 값으로 덮어써 고아 메시지를 만들지 않는다.
- 승인 producer는 공용 생명주기 파사드를 사용한다. 파사드는 lease, 기존 상태 확인, 안전한 교체, 원장 기록, 게시를 하나의 임계구역으로 처리한다.
- 승인된 요청은 워처가 소비한다. 새 게시자가 승인·거부된 레코드를 변경하거나 다시 게시해서는 안 된다.

## 워처와 라우팅

- cron 워처는 승인 **리액션만** 폴링한다. 메시지·첨부 수신은 실시간 에이전트의 단일 소유 영역이며, 두 소비자가 경쟁하면 중복 실행이 생긴다.
- 행동 없는 수동 인덱서·관측 작업은 예외가 될 수 있지만, 그 예외는 외부효과가 없음을 코드로 보장해야 한다.
- no-agent cron은 필요한 자격증명을 보호된 환경에서 명시적으로 읽고, 자식 프로세스에 필요한 값만 `env=`로 전달한다. 상속 여부에 의존하지 않는다.
- 새 승인 표면은 기존 게이트의 초안·pending·render·resolve·watch 흐름을 재사용한다. 표면 차이는 저장 레코드의 채널 바인딩으로 표현하며 채널별 병렬 워처를 만들지 않는다.
- 중첩 도메인의 변경 경로에는 양방향 결정론적 가드가 필요하다. 자연어 라우팅만 믿으면 같은 요청이 두 경로로 실행될 수 있다. 정확히 하나로 분류할 수 없으면 clarify로 끝낸다.
- 워처 파일명과 배포 대상 이름은 런타임 디렉터리 안에서 스킬별로 고유해야 한다.

## 배포와 운영

- 배포 입력은 승인된 원격 기준과 바이트 단위로 일치해야 한다. 미커밋·미게시·뒤처진 체크아웃에서의 배포는 차단한다. 실험용 예외는 명시적으로 격리하고 정상 배포의 우회 수단으로 사용하지 않는다.
- 일반 순서는 **검토 가능한 변경 → 원격 반영 → 배포 → 실제 검증**이다. 문서·테스트만 바뀐 경우에도 검토와 원격 반영은 필요하다.
- 배포 체크아웃은 원격 기준의 단방향 미러다. 그 안에서는 fetch와 fast-forward 수렴만 허용하고 편집·커밋·stash를 금지한다. 미러의 변경은 다음 수렴에서 조용히 유실되거나 다른 작업자의 수렴을 막을 수 있다.
- 미러 상태는 가끔 수동으로 추정하지 말고 dirty·ahead·behind를 비파괴적으로 감지한다. 복구 전에는 보존할 커밋과 작업물을 먼저 추출하고, 파괴적 reset을 첫 선택으로 쓰지 않는다.
- 활성 불변 릴리스가 존재하면 그 릴리스가 운영 권위 소스다. 체크아웃 미러의 경고와 활성 릴리스의 손상을 구분하되, 깨진 활성 포인터를 정상적인 부재로 취급하지 않는다.
- 게이트웨이를 재시작해야 한다면 원인을 먼저 확인하고, 상호의존하는 모든 게이트웨이 구성원을 일관된 집합으로 재시작한다. 진행 중인 승인 요청은 fail-closed 시간 초과가 될 수 있으므로 재시작 뒤 다시 요청한다.
- 수리 에이전트는 격리된 작업 클론에서만 패치를 만들고 검증한다. 전용 수리 브랜치로 push한 뒤 검토용 pull request를 만들며, 기본 브랜치는 소유자(owner)가 검토 후 병합한다.
- 수리 승인은 패치 이름이 아니라 패치 바이트와 변경 요약에 결속한다. 승인 뒤 바이트가 바뀌면 적용을 중단하고 감사상 명확한 실패로 남긴다.

## 변경 위생

- 병렬 작업 환경에서는 현재 저장소와 대상 영역의 최신 결정을 먼저 확인한다. 확정된 설계와 충돌하면 이전 이해로 되돌리지 말고 근거를 검토하거나 소유자(owner)에게 묻는다.
- 다른 작업자의 미커밋 변경은 복원 가능한 명시적 승인 없이는 stash, reset, checkout, discard하지 않는다.
- 논리적 변경 단위마다 독립적으로 검토하고 커밋한다. 서로 무관한 변경을 섞지 않는다.
- stage 전에는 변경 파일 목록과 삽입·삭제 방향을 검토한다. 특히 문서 추가가 순삭제로 보이거나 버전이 낮아지면 멈추고 정본과 대조한다.
- 공개 기능 인벤토리는 내부 계획·세션·운영 백로그의 대체물이 아니다. 범위 밖의 후속 작업은 비공개 계획 또는 이슈 추적기에 기록한다.

## 문서 규약

- 이 저장소의 설명 문서는 한국어를 기본으로 하되, 코드 식별자와 상호운용 계약은 필요한 언어를 그대로 쓴다.
- 기능 소개는 무엇을 하는지, 왜 필요한지, 합성된 사용 시나리오, 관련 공개 경로를 짧게 제공한다. 원문 메시지, 실제 수신자, 운영 로그, 자격증명, 사설 식별자는 넣지 않는다.
- 운영 세부는 환경 변수와 예시 값으로 표현한다. 실제 호스트명·주소·계정·경로·채널 식별자·소유자 식별자는 공개 문서에 기록하지 않는다.

## 금지 사항

- 주석·문서·테스트 픽스처에 실제 토큰이나 토큰처럼 보이는 민감 문자열을 넣지 않는다. 비밀 스캐너의 오탐도 배포를 중단시킬 수 있다.
- 프로덕션 게이트웨이에 테스트 모드를 켜지 않는다.
- 승인 없이 외부효과를 직접 호출하지 않는다.
- 공개 트리에서 제거된 공급자 전용 구현이나 개인 운영 기록을 다시 참조하지 않는다.

## 검증 명령

체크아웃만 있으면 실행되는 검사다. CI도 같은 명령을 실행한다.

```bash
python3 -m pytest tests/unit
python3 tools/repo_scan.py --profile public-generic --root .
python3 tools/repo_scan.py --profile docs-claims --root .
```

`pytest tests/unit` 대신 `python3 -m pytest tests/unit`을 쓴다 — 전자는 새 체크아웃에서 저장소 루트가 `sys.path`에 없어 `tests` 패키지를 import하지 못한다.

아래는 검증 명령이 아니라 **실제 인프라를 전제로 하는 운영 명령**이다. 전제가 없으면 fail-closed로 거부되므로 위생 게이트의 일부로 실행하지 않는다.

```bash
ruff check .                          # 선택. 루트에 패키지 매니페스트가 없어 ruff 기본값으로 동작하며 CI 필수 단계가 아니다
automation/deploy-skill.sh <skill>    # 배포 노드·계정·승인 채널 필요
automation/healthcheck.sh             # HEALTHCHECK_* 환경 변수와 원격 호스트 필요
python3 -m automation.rag_ingest      # 검색 스택과 인제스트 원본 필요
```

`tests/e2e/`의 시나리오 뱅크는 단위 스위트에 포함되지 않는다. 오프라인 시나리오만 추가 설정 없이 돌고, 인프라 의존 시나리오는 전제 조건이 없으면 exit 77로 skip한다 — 뱅크가 통과했다는 것이 전체 경로를 실행했다는 뜻은 아니다.
